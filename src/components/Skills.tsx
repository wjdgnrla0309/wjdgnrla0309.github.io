import { motion } from 'framer-motion';import { skillGroups } from '../data/skills'
import { useLanguage } from '../LanguageContext'
export function Skills(){const { language }=useLanguage();const ko=language==='ko';return <section id="skills" className="section-shell section-block"><div className="section-heading"><p className="eyebrow">05 / {ko?'역량':'CAPABILITIES'}</p><h2>{ko?<>공학의 기초와<br/>실무 도구.</>:<>Engineering foundation<br/>&amp; practical tools.</>}</h2></div><div className="skills-list">{skillGroups.map((g,i)=><motion.article className="skill-row" key={g.title} initial={{opacity:0,y:15}} whileInView={{opacity:1,y:0}} viewport={{once:true}}><span>0{i+1}</span><h3>{ko?['공학','계산 및 개발','도구','연구'][i]:g.title}</h3><p>{g.items.join('  /  ')}</p></motion.article>)}</div></section>}
