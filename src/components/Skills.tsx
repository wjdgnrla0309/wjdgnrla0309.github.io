import { motion } from 'framer-motion';import { skillGroups } from '../data/skills'
export function Skills(){return <section id="skills" className="section-shell section-block"><div className="section-heading"><p className="eyebrow">05 / CAPABILITIES</p><h2>Engineering foundation<br/>&amp; practical tools.</h2></div><div className="skills-list">{skillGroups.map((g,i)=><motion.article className="skill-row" key={g.title} initial={{opacity:0,y:15}} whileInView={{opacity:1,y:0}} viewport={{once:true}}><span>0{i+1}</span><h3>{g.title}</h3><p>{g.items.join('  /  ')}</p></motion.article>)}</div></section>}
