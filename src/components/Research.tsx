import { motion } from 'framer-motion'
import { researchTopics } from '../data/research'
import { useLanguage } from '../LanguageContext'
export function Research() { const { language } = useLanguage(); const ko = language === 'ko'; return <section id="research" className="section-shell section-block"><div className="section-heading"><p className="eyebrow">02 / {ko ? '연구' : 'RESEARCH'}</p><h2>{ko ? <>미래 모빌리티를 위한<br/>열관리 시스템.</> : <>Thermal systems for<br/>future mobility.</>}</h2></div><div className="focus-list">{researchTopics.map(({title,description,icon:Icon},i)=><motion.article key={title} className="focus-row" initial={{opacity:0,y:16}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.05}}><span className="row-index">0{i+1}</span><Icon size={25}/><div><h3>{title}</h3><p>{description}</p></div></motion.article>)}</div><div className="system-map" aria-label={ko ? '전기차 열관리 시스템 흐름' : 'Electric vehicle thermal management system flow'}><span>{ko ? '배터리' : 'BATTERY'}</span><i className="cool"/><span>{ko ? '냉각수' : 'COOLANT'}</span><i className="cool"/><span>{ko ? '라디에이터' : 'RADIATOR'}</span><i className="heat"/><span>{ko ? '히트펌프' : 'HEAT PUMP'}</span><i className="heat"/><span>{ko ? '실내' : 'CABIN'}</span></div></section> }
